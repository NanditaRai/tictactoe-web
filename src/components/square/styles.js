import { css } from '@emotion/core';
import styled from '@emotion/styled';

export const squares = css`
  background: #272727;
  border: none;
  font-size: 16px;
  cursor: pointer;
  outline: none;
  color: #fff;
`;
